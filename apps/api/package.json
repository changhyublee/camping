{
  "name": "@camping/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@camping/shared": "workspace:*",
    "@fastify/cors": "^11.0.0",
    "fastify": "^5.0.0",
    "openai": "^4.0.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.0.7"
  }
}
