# schemas

분석 모델 출력과 관련된 JSON 스키마를 두는 경로다.

## 현재 파일

- `codex-trip-analysis-output.schema.json`
  - 로컬 `codex` CLI 출력 형식을 제한할 때 사용하는 스키마

## 참고

- YAML 운영 데이터 검증 스키마는 이 디렉토리가 아니라 `shared/src/schemas.ts` 에 있다.
